
(ns re-frame.dev.state)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @ignore
;
; @atom (integer)
(def DB-RESET-COUNT (atom 0))

; @atom (boolean)
(def DEBUG-MODE? (atom false))
